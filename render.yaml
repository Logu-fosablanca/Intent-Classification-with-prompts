services:
  - type: web
    name: intent-classifier-demo
    env: docker
    dockerfilePath: ./web/Dockerfile
    dockerContext: .
    plan: free
    envVars:
      - key: PORT
        value: 8000
    healthCheckPath: /
